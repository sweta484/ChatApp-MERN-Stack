{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "nodemon ./server/init.js",
    "server": "nodemon ./server/index.js",
    "dev": "concurrently --kill-others \"npm run server\"  \"npm run start --prefix ./client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sweta Sadhya",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.1.0",
    "nodemon": "^2.0.7"
  }
}
